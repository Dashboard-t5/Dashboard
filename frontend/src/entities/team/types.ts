export type TTeam = {
    employee_id: number,
    employee: string,
    total_yes: number,
    total: number,
    percentage: number,
    bus_factor: number | boolean,
}

export type TTeamName = {
    id: number,
    name: string,
}